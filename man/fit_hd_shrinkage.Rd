% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-hd-shrinkage.R
\name{fit_hd_shrinkage}
\alias{fit_hd_shrinkage}
\title{Restriction-adaptive shrinkage in a high-dimensional linear model}
\usage{
fit_hd_shrinkage(
  X,
  y,
  restricted_set,
  C = NULL,
  t = NULL,
  endpoint = c("regularized", "debiased_models"),
  lambda = NULL,
  lambda_node = NULL,
  test = c("auto", "wald", "max"),
  precision = c("nodewise", "diagonal", "user"),
  theta_rows = NULL,
  precision_fit = NULL,
  full_model_precision_fit = NULL,
  submodel_precision_fit = NULL,
  assume_independent = FALSE,
  alpha = 0.05,
  bootstrap_B = 999L,
  bootstrap_seed = NULL,
  bootstrap_multipliers = NULL,
  bootstrap_block_size = 256L,
  fixed_q_max = 10L,
  penalty_factor = NULL,
  max_iter = 30000L,
  tol = 5e-06,
  check_every = 50L,
  verbose = FALSE,
  full_beta_init = NULL,
  restricted_beta_init = NULL,
  full_operator_norm = NULL,
  restricted_operator_norm = NULL,
  epsilon = 1e-10
)
}
\arguments{
\item{X}{Numeric \code{n} by \code{p} design matrix.}

\item{y}{Numeric response vector of length \code{n}.}

\item{restricted_set}{One-based indices \code{M} involved in the restriction.}

\item{C}{Optional \code{q} by \code{length(M)} contrast matrix. \code{NULL} means the
coordinate restriction \code{beta[M] = t}, whose projection is implemented
without constructing a \code{q} by \code{q} matrix.}

\item{t}{Null target. It defaults to zero.}

\item{endpoint}{Shrinkage endpoints. \code{"regularized"} uses the full
square-root lasso and its restriction projection. \code{"debiased_models"}
computes one full-model debiased estimator and projects it to the
coordinate zero subspace. The latter currently requires a coordinate
zero-block restriction.}

\item{lambda}{Square-root lasso penalty.}

\item{lambda_node}{Nodewise lasso penalty.}

\item{test}{One of \code{"auto"}, \code{"wald"}, or \code{"max"}. Automatic selection
uses Wald only when \code{q <= fixed_q_max}.}

\item{precision}{One of \code{"nodewise"}, \code{"diagonal"}, or \code{"user"}.}

\item{theta_rows}{User precision rows for \code{precision = "user"}.}

\item{precision_fit}{Optional reusable result from
\code{\link[=estimate_precision_rows]{estimate_precision_rows()}}.}

\item{full_model_precision_fit}{Optional reusable all-row precision result
for the standardized full design when \code{endpoint = "debiased_models"}.}

\item{submodel_precision_fit}{Deprecated compatibility argument. It is
ignored because no separate submodel debiasing fit is performed.}

\item{assume_independent}{Required for diagonal precision. This is suitable
for a design whose diagonal population precision is assumed in advance,
such as the Gaussian iid simulation design.}

\item{alpha}{Test level.}

\item{bootstrap_B}{Number of multiplier draws for the maximum test.}

\item{bootstrap_seed}{Optional seed used without changing the caller's RNG
state.}

\item{bootstrap_multipliers}{Optional reusable \code{n} by \code{bootstrap_B}
Gaussian multiplier matrix.}

\item{bootstrap_block_size}{Number of multiplier draws processed in one C++
block.}

\item{fixed_q_max}{Largest restriction dimension selected for Wald by
\code{test = "auto"}.}

\item{penalty_factor}{Nonnegative square-root lasso penalty factors.}

\item{max_iter}{Maximum primal-dual iterations.}

\item{tol}{Solver tolerance.}

\item{check_every}{Iteration interval for convergence diagnostics.}

\item{verbose}{Print periodic solver diagnostics.}

\item{full_beta_init}{Optional original-scale warm start for the full fit.}

\item{restricted_beta_init}{Deprecated compatibility argument. It is
ignored because the restricted endpoint is projected from the full fit.}

\item{full_operator_norm}{Optional reusable full-design operator norm.}

\item{restricted_operator_norm}{Deprecated compatibility argument. It is
ignored because no restricted-design solver is run.}

\item{epsilon}{Positive numerical floor used only in a shrinkage denominator.}
}
\value{
An object of class \code{hd_shrinkage_fit} containing the regularized and
selected full/submodel endpoints, the available adaptive estimators, full
solver and projection diagnostics, precision directions, and inference
output.
}
\description{
\code{fit_hd_shrinkage()} fits one full square-root lasso with all columns of
\code{X}. The submodel endpoint is the restriction projection of that full
estimator; it is never re-fitted on a reduced design. The endpoints can be
the regularized full/projection pair or, for a zero-coordinate restriction,
a full one-step debiased estimator and its projection. A debiased Wald
statistic is available for fixed, small restriction dimension. A
multiplier-bootstrap maximum test is used for a large coordinate block and
does not form a \code{q} by \code{q} covariance matrix. Neither branch uses ordinary,
ridge, or generalized ridge regression.
}
\details{
Let \verb{A beta = t} denote the restriction and let \code{V} denote the projection
geometry. The conceptual restricted endpoint is
\deqn{\hat\beta^{SM}=\hat\beta^F-
V A^T(A V A^T)^\dagger(A\hat\beta^F-t).}
The implementation uses Euclidean geometry after internal column
standardization. For \code{beta[M] = t}, this simply replaces the selected
coordinates of the all-\code{X} full estimate by \code{t}; all other coordinates are
inherited exactly from the full fit.

For a large coordinate restriction, the max-calibrated estimators are
\deqn{\hat\beta^{S_\infty}=\hat\beta^{SM}+
(1-a^*/(T_{\max}^2\vee\epsilon))
(\hat\beta^{FM}-\hat\beta^{SM})}
and its positive-part version. The separately reported protected version
equals the full endpoint when the max test rejects and otherwise equals the
positive-part estimator. Here \verb{a*} is the conditional mean of the
squared multiplier-bootstrap maximum. These estimators are experimental;
the package does not assert classical James--Stein dominance for them.

The restricted set must be specified independently of the response used for
inference. If it is selected from data, sample splitting or external
information is required for the advertised test interpretation.
}
\examples{
set.seed(1)
X <- matrix(rnorm(40 * 70), 40, 70)
beta <- c(rep(1, 4), rep(0, 66))
y <- drop(X \%*\% beta + rnorm(40))
fit <- fit_hd_shrinkage(
  X, y,
  restricted_set = 5:70,
  test = "max",
  precision = "diagonal",
  assume_independent = TRUE,
  bootstrap_B = 99,
  max_iter = 5000,
  tol = 1e-4
)
coef(fit, method = "PS")
coef(fit, method = "PPS")

}
